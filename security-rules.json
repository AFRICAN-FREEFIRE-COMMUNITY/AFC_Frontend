{
  "$comment": "How AFC_Frontend meets owner rules R55 to R61 (2026-09-17). A Next.js client of the AFC-B API: no database, no paid service, no login handler of its own. Secrets never live here (R55); the API's ownership rules and their runtime IDOR cases live in AFC-B/security/. Read by ~/.claude/skills/security-rules/scripts/check-security.mjs.",
  "frontend": [
    "app/",
    "components/",
    "lib/",
    "contexts/",
    "hooks/"
  ],
  "server": [],
  "ignore": [
    ".next/",
    ".next-*/",
    "node_modules/",
    "public/downloads/"
  ],
  "db": null,
  "migrations": [],
  "rls": null,
  "rateLimit": {
    "middleware": [],
    "edge": [],
    "live": null
  },
  "ownership": {
    "markers": [],
    "cases": "security/idor-cases.json"
  },
  "billing": {
    "register": "security/billing-caps.md",
    "maxAgeDays": 90
  },
  "allow": [
    {
      "rule": "R61",
      "path": "app/(organizer)/organizer/ai-key/page.tsx",
      "reason": "not SQL: a next-intl message key built from the provider id (t.raw(`guides.${provider.id}.steps`)); nothing runs as a query or a command"
    },
    {
      "rule": "R55",
      "path": ".env",
      "reason": "tracked on purpose: it holds only NEXT_PUBLIC_* values (the API origin, the site origin, the Google client id, feature flags), which ship in the bundle anyway; a secret never goes here, .env.local is git-ignored for those"
    },
    {
      "rule": "R62",
      "path": ".env",
      "reason": "tracked on purpose and holds only NEXT_PUBLIC_* values (the API origin, the site origin, a Google OAuth client id, which is public by design, and two feature flags); git log -p over every commit of the file shows no other key ever in it (checked 2026-09-18); secrets live in .env.local, which is ignored"
    }
  ],
  "https": {
    "enforcedBy": "nginx on the VPS, in the backend repo's deploy kit (AFC-B deploy/vps/nginx-afc.conf): port 80 answers 301 to https; HSTS max-age=31536000 includeSubDomains on the site and the API host; read back live 2026-09-18",
    "verified": "2026-09-18"
  }
}
